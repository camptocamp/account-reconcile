<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <data noupdate="1">
    <record id="todo_filter" model="ir.filters">
      <field name="name">ToDo</field>
      <field name="model_id">crm.claim</field>
      <field name="domain">['|', ('last_message_date', '&lt;=', (context_today() + datetime.timedelta(days=-2)).strftime('%Y-%m-%d')), ('last_message_date', '=', False')]</field>
      <field name="user_id" eval="False"/>
      <field name="action_id" ref="crm_claim.crm_claim_category_claim0"/>
    </record>
  </data>

</odoo>
