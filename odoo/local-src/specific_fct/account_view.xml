<?xml version="1.0" encoding="utf-8"?>
<openerp>
    <data noupdate="0">
        <record id="view_account_move_line_filter" model="ir.ui.view">
            <field name="name">account.move.line.select</field>
            <field name="model">account.move.line</field>
            <field name="inherit_id" ref="account_statement_ext.view_account_move_line_filter_add_statement"/>
            <field name="arch" type="xml">
                <filter string="Bank Statement" position="after">
                    <filter string="Partial Reconciliation Number"
                            context="{'group_by': 'reconcile_partial_id'}"
                            icon="terp-partner"/>
                </filter>
            </field>
        </record>
    </data>
</openerp>
