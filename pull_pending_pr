set -v
set -e

cd parts/bank-statement-reconcile
git pull --no-edit origin pull/83/head # New module account_statement_account_partner_import
cd -

cd parts/server
git pull --no-edit origin pull/142/head # Fix for manual entries posting
cd -

set +v
set +e
