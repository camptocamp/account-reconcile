set -v
set -e

cd parts/bank-statement-reconcile
git pull --no-edit origin pull/83/head # New module account_statement_account_partner_import
cd -

cd parts/server
git pull --no-edit origin pull/124/head # Fix for mail: show original message
git pull --no-edit origin pull/142/head # Fix for manual entries posting
git pull --no-edit origin pull/145/head # Fix for decimal precision in write-off
cd -

cd parts/server-tools
git pull --no-edit origin pull/144/head # Fix for SQL concurrency
cd -

cd parts/e-commerce
git pull --no-edit origin pull/37/head # Check invoice account for auto workflow
cd -

cd parts/web
git pull --no-edit origin pull/144/head # add module 'web_warning_on_save'
cd -

set +v
set +e
